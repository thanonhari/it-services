# Stack & operations — {{DOMAIN_NAME}}

## Core tools

- Primary craft apps: {{EXTERNAL_APPS}}
- In-session gen: {{PRIMARY_GEN_TOOLS}}
- Agent models: `{{STRONG_MODEL}}` / `{{FAST_MODEL}}`

## Commands (fill real ones)

```bash
# install / bootstrap
# build / export
# test / validate
```

## Secrets & config (names only)

- List env var **names** and where they live (dashboard, Wrangler, 1Password).
- Never paste secret values into wiki.

## Hosting / publish targets

| Target | Role |
|--------|------|
| (url or store) | (what ships there) |

## Naming

- `{{EXPORT_NAMING}}`
